import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adrianamat.dancecounter',
  appName: 'dance-counter',
  webDir: 'www'
};

export default config;
